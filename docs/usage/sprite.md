# Sprite

command: `termixel sprite <sprite_name>`

## Usage

Renders the sprite provided by the user into the terminal using unicode characters.
> Warn: If the sprite is too large, it may not fit in the terminal.

The sprite name is the name of the sprite file to render without the file extension (.png).
